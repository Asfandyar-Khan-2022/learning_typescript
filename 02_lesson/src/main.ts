let myName: string;
let album: any;

myName = "Dave"
myName = "John"
album = 5150

const sum = (a: number, b: string) => {
    return a + b
}

let postId: string | number;
let isActive: number | boolean | string;

let re: RegExp = /\w+/g